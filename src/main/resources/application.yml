server:
  port: 9529
spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql:///hr_admin
  redis:
    port: 6379
    host: localhost

logging:
  level:
    com.edu: debug

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  type-aliases-package: com.edu.*.entity
